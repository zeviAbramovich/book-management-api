version: '3.8'

services:
  book-management-api:
    build: .
    image: book-management-api:dev
    container_name: book-management-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev